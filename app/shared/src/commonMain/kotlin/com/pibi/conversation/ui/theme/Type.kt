package com.pibi.conversation.ui.theme

import androidx.compose.material3.Typography

/**
 * The Material 3 default type scale, as exported by Material Theme Builder — no custom fonts were
 * chosen there. Override individual styles here (`Typography(bodyLarge = TextStyle(...))`) to
 * change type across the app; every composable reads them through `MaterialTheme.typography`.
 */
val AppTypography = Typography()
